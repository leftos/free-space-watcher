using System.Windows;

namespace FreeSpaceWatcher.Tray;

/// <summary>The tray application; it has no main window and runs until shut down explicitly.</summary>
public partial class App : Application { }
